# admin.pyfrom datetime import datetimefrom aiogram import Router, Ffrom aiogram.filters import Commandfrom aiogram.fsm.context import FSMContextfrom aiogram.fsm.state import StatesGroup, Statefrom aiogram.types import (    Message,    CallbackQuery,    InlineKeyboardMarkup,    InlineKeyboardButton,)from app.database.sqlite_db import (    get_all_waiters,    get_waiter_by_tg,    add_shift,    set_shift_hours,    set_shift_tasks,)from app.utils.calendar import make_calendaradmin = Router()# --- Admin IDs ---ADMIN_ID = {2015462319, 1773695867}# --- FSM states ---class AdminStates(StatesGroup):    SET_HOURS_CAL = State()    WAIT_HOURS = State()    EDIT_SELECT_W = State()    EDIT_CAL = State()    WAIT_TASKS = State()# --- Helper ---def is_admin(tg_id: int) -> bool:    return tg_id in ADMIN_ID# --- Main admin menu ---@admin.message(Command("admin_menu"))async def cmd_admin_menu(message: Message, state: FSMContext):    if not is_admin(message.from_user.id):        return    await state.clear()    kb = InlineKeyboardMarkup(inline_keyboard=[        [InlineKeyboardButton(text="🕒 Установить часы всем", callback_data="AM_SET_HOURS")],        [InlineKeyboardButton(text="🗓 Редактировать график", callback_data="AM_EDIT_SCHEDULE")],        [InlineKeyboardButton(text="💰 Рассчитать зарплату", callback_data="AM_CALC_SALARY")],        [InlineKeyboardButton(text="📢 Оповестить", callback_data="AM_NOTIFY")],        [InlineKeyboardButton(text="📥 Скачать Excel", callback_data="AM_EXPORT")],    ])    await message.answer("<b>Меню администратора</b>", parse_mode="HTML", reply_markup=kb)# --- Global hours calendar ---@admin.callback_query(F.data == "AM_SET_HOURS")async def set_hours_start(query: CallbackQuery, state: FSMContext):    if not is_admin(query.from_user.id): return    year, month = datetime.today().year, datetime.today().month    cal = make_calendar(year, month, set())    cal.inline_keyboard.append([        InlineKeyboardButton(text="⏪ Назад", callback_data="AM_BACK_MENU")    ])    await state.set_state(AdminStates.SET_HOURS_CAL)    await query.message.edit_text("Выберите дату для часов всем:", reply_markup=cal)@admin.callback_query(F.data.startswith("CAL_DAY|"), F.state(AdminStates.SET_HOURS_CAL))async def set_hours_date(query: CallbackQuery, state: FSMContext):    _, date = query.data.split("|",1)    await state.update_data(date=date)    kb = InlineKeyboardMarkup(inline_keyboard=[        [InlineKeyboardButton(text="⏪ Назад", callback_data="AM_BACK_MENU")]    ])    await state.set_state(AdminStates.WAIT_HOURS)    await query.message.edit_text(f"Введите часы для всех на {date}:", reply_markup=kb)@admin.message(F.state(AdminStates.WAIT_HOURS))async def set_hours_input(message: Message, state: FSMContext):    data = await state.get_data()    date = data.get("date")    try:        hours = float(message.text.replace(",","."))    except ValueError:        return await message.answer("Введите число часов.")    for tg in get_all_waiters():        add_shift(tg, date)        set_shift_hours(tg, date, hours)    kb = InlineKeyboardMarkup(inline_keyboard=[        [InlineKeyboardButton(text="⏪ Назад", callback_data="AM_BACK_MENU")]    ])    await message.answer(f"Сохранено: {hours} ч. всем на {date}", reply_markup=kb)    await state.clear()# --- Personal schedule edit ---# --- Personal schedule edit: calendar first ---@admin.callback_query(F.data == "AM_EDIT_SCHEDULE")async def edit_schedule_start(query: CallbackQuery, state: FSMContext):    if not is_admin(query.from_user.id):        return    # Show calendar to pick a date    year, month = datetime.today().year, datetime.today().month    cal = make_calendar(year, month, set())    # add back to menu    cal.inline_keyboard.append([        InlineKeyboardButton(text="⏪ Назад", callback_data="AM_BACK_MENU")    ])    await state.set_state(AdminStates.EDIT_CAL)    await query.message.edit_text("Выберите дату для редактирования графика:", reply_markup=cal)# --- After date selected, choose worker ---@admin.callback_query(F.data.startswith("CAL_DAY|"), F.state(AdminStates.EDIT_CAL))async def edit_schedule_date(query: CallbackQuery, state: FSMContext):    _, date_str = query.data.split("|",1)    # store date    await state.update_data(date=date_str)    # build worker list by name    waiters = get_all_waiters()    buttons = []    for tg in waiters:        wid, name = get_waiter_by_tg(tg)        label = name or f"ID:{tg}"        buttons.append([InlineKeyboardButton(text=label, callback_data=f"AM_SELECT_W|{tg}")])    buttons.append([InlineKeyboardButton(text="⏪ Назад к календарю", callback_data="AM_EDIT_SCHEDULE")])    kb = InlineKeyboardMarkup(inline_keyboard=buttons)    await state.set_state(AdminStates.EDIT_SELECT_W)    await query.message.edit_text(f"Дата: {date_str}. Выберите сотрудника:", reply_markup=kb)# --- After worker selected, choose to-do or no tasks ---@admin.callback_query(F.data.startswith("AM_SELECT_W|"), F.state(AdminStates.EDIT_SELECT_W))async def edit_schedule_worker(query: CallbackQuery, state: FSMContext):    _, tg = query.data.split("|",1)    await state.update_data(tg=int(tg))    data = await state.get_data()    date_str = data.get("date")    # menu: to-do or no tasks    kb = InlineKeyboardMarkup(inline_keyboard=[        [InlineKeyboardButton(text="📝 Прописать задачи", callback_data="AM_ENTER_TASKS")],        [InlineKeyboardButton(text="❌ Без задач", callback_data="AM_NO_TASKS")],        [InlineKeyboardButton(text="⏪ Назад к выбору сотрудника", callback_data=f"CAL_DAY|{date_str}")],    ])    await state.set_state(AdminStates.ENTER_TASKS)    await query.message.edit_text(f"{date_str}, сотрудник {tg}. Задачи?", reply_markup=kb)# --- Enter tasks ---@admin.callback_query(F.data == "AM_ENTER_TASKS", F.state(AdminStates.ENTER_TASKS))async def edit_schedule_enter_tasks(query: CallbackQuery, state: FSMContext):    await state.set_state(AdminStates.WAIT_HOURS)    await query.message.edit_text("Введите список задач:")# --- Receive tasks input ---@admin.message(F.state(AdminStates.WAIT_HOURS))async def edit_schedule_tasks_input(message: Message, state: FSMContext):    data = await state.get_data()    tg = data.get("tg")    date_str = data.get("date")    tasks = message.text.strip()    add_shift(tg, date_str)    set_shift_tasks(tg, date_str, tasks)    kb = InlineKeyboardMarkup(inline_keyboard=[        [InlineKeyboardButton(text="⏪ Назад в меню", callback_data="AM_BACK_MENU")]    ])    await message.answer("Задачи сохранены.", reply_markup=kb)    await state.clear()# --- No tasks ---@admin.callback_query(F.data == "AM_NO_TASKS", F.state(AdminStates.ENTER_TASKS))async def edit_schedule_no_tasks(query: CallbackQuery, state: FSMContext):    data = await state.get_data()    tg = data.get("tg")    date_str = data.get("date")    add_shift(tg, date_str)    set_shift_tasks(tg, date_str, "")    # back to main menu    await back_menu(query, state)# --- Back to menu ---@admin.callback_query(F.data == "AM_BACK_MENU")async def back_menu(query: CallbackQuery, state: FSMContext):    await state.clear()    await cmd_admin_menu(query.message, state)# Other handlers (set hours global, calc salary, notify, export) remain unchanged.